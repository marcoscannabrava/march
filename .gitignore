# Claude Code secrets — never version these
config/_home/.claude/.credentials.json
config/_home/.claude/*.credentials.json

# Codex credentials — never version these
config/_home/.codex/auth.json

# pi credentials — never version these
config/_home/.pi/agent/auth.json

notes
